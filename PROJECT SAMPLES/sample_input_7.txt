// test complex if

IF a > b:
   PRINT("heat from fire");//just a test
ENDIF;

IF a >= b AND c <= D AND E != F: //just another test
   PRINT("when sunlight strikes raindrops in the air");
ENDIF;
IF a != b OR c <= D OR E != F OR F = 9.8E+7+6.5e4+3e+21:
   PRINT("milkshakes are tasty");
ENDIF;

// mixture of and or
IF a <= b AND c >= D OR e != F:
   PRINT("fire from heat");
ENDIF;

PRINT("sovte");